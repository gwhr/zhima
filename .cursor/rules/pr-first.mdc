---
description: 任何功能改动都必须先通过 PR 流程，不允许直接在主分支合并功能变更。
alwaysApply: true
---

# PR First Rule

## 强制规则
- 任何功能改动（新增功能、功能调整、行为变更、接口变更）都必须先提交 PR。
- PR 必须包含：
  - 变更说明（做了什么、为什么做）
  - 测试或自测结果
  - 关联的 openspec 变更文档
- 未走 PR 的功能改动视为未完成，不允许宣称交付完成。

## 建议流程
1. 先更新 openspec 变更文档。
2. 在功能分支提交代码。
3. 发起 PR，完成 review 后再合并。
