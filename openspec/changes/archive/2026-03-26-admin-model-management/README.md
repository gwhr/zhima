# 2026-03-26 Admin Model Management

This archive records the admin model-management enhancement:

1. manage provider API keys from admin UI
2. choose code/thesis generation models from admin UI
3. use admin-managed keys at runtime (with env fallback)

Archive unit:

- `admin-model-management`
