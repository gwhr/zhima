# 2026-03-25 Risk Control Thresholds

This archive records admin risk-control thresholds for task execution:

- Per-user concurrency limit override (admin configurable)
- Platform-level retry limit for same task type
- Platform-level hard token cap per single task

Archive unit:

- `task-risk-control`
