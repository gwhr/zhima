# 2026-03-26 Model Connectivity Test

This archive records the admin model connectivity testing enhancement:

1. add model test API for built-in and custom models
2. add per-model "test connection" action in admin model page
3. support testing with unsaved key/baseUrl overrides

Archive unit:

- `model-connectivity-test`

